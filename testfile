east-1-dev1: dev1-east1.yml
east-2-dev1: dev1-east2.yml
dev-east: dev-east.yml
dev-west: dev-west.yml
qa-east: qa-east.yml
qa-west: qa-west.yml
uat-east: uat-east.yml
uat-west: uat-west.yml
prod-dal: prod-dal.yml
prod-phx: prod-phx.yml




-------------

// vars/getFoundationManifests.groovy
def call(String pcfFoundation, String fileType = "manifest") {
    def yamlText = libraryResource('pipeline-global-config/foundation-manifests.yml')
    def foundationManifests = readYaml text: yamlText

    def manifestList = foundationManifests[pcfFoundation]

    if (!manifestList) {
        error "❌ No mapping found for PCF foundation: ${pcfFoundation}"
    }

    // Ensure list format
    if (manifestList instanceof String) {
        manifestList = [manifestList]
    }

    // Add prefix dynamically based on file type
    manifestList = manifestList.collect { fileName ->
        return "${fileType}-${fileName}"
    }

    echo "✅ Resolved ${fileType} file(s) for ${pcfFoundation}: ${manifestList}"
    return manifestList
}


-------------

in jenkins file, the pipeline using manifest use
def manifestList = getFoundationManifests(pcfFoundation)

whihc uses vars

def varsList = getFoundationManifests(pcfFoundation, "vars")
